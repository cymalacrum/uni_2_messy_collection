//removed the method for lighting up the links from the main.js file, they dont need to glow when not selected, may add singular link light up later, not node light up 
//this would be to show where you are going
    /*.onLinkHover(link => {
      highlightNodes.clear();
      highlightLinks.clear();

      if (link) {
        highlightLinks.add(link);
        highlightNodes.add(link.source);
        highlightNodes.add(link.target);
      }

      updateHighlight();
      })*/
  